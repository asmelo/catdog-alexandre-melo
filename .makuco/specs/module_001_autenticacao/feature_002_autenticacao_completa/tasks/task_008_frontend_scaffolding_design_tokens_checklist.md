# Makuco Codegen Checklist: TASK-FRONTEND-008 — Scaffolding do frontend e design system a partir do reference.html

**Purpose**: Validar a qualidade da geração de código do slice inaugural do frontend (React 18 + Vite 5 + Tailwind 3 + tradução dos design tokens do `reference.html`). Cada item abaixo reflete uma verificação **efetivamente executada**; itens não executáveis neste ambiente estão marcados como `[~]` (N/A) ou `[!]` (bloqueado) com a justificativa, e **nunca** como concluídos.
**Created**: 2026-08-22
**Feature**: [spec_context.md](../spec_context.md)
**Prompt Plan**: [task_008_frontend_scaffolding_design_tokens.md](./task_008_frontend_scaffolding_design_tokens.md)

**Legenda**: `[x]` verificado e aprovado · `[~]` não aplicável a este slice (com justificativa) · `[!]` bloqueado por ambiente (com justificativa)

**Ambiente de verificação**: Node **v20.20.2** / npm **10.8.2** (`nvm use 20`, coerente com o `.nvmrc` da raiz e com `engines: {node: ">=20 <21"}`). `npm install` sem nenhum `EBADENGINE`: 136 pacotes. Navegador via Playwright (Chromium), backend real na 3333 (`node dist/index.js` com o `.env` do Supabase).

## Quality Tools

- [x] Run linters and compilers available in the project to ensure the generated code is free of errors and follows the project's standards.
  - `npm run typecheck` (`tsc --noEmit` com `strict`, `noImplicitAny`, `noUncheckedIndexedAccess`, `noUnusedLocals`, `noUnusedParameters`, `noFallthroughCasesInSwitch`): **0 erros**.
  - `npm run build` (`tsc --noEmit && vite build`): **0 erros**, 31 módulos transformados, `dist/` gerado (`index.html` 1.20 kB, CSS 6.99 kB, JS 143.18 kB).
  - ESLint: **não existe no projeto** — não faz parte do contrato de dependências desta task (a suíte e as ferramentas de teste são a TASK-FRONTEND-013). O MCP `quality-check` confirmou a ausência (`ESLint couldn't find an eslint.config.(js|mjs|cjs)`). Compensado por execução **efêmera** de `eslint@9.39.5` + `typescript-eslint@8` (`recommendedTypeChecked` + `stylisticTypeChecked`) + `eslint-plugin-react@7` + `eslint-plugin-react-hooks@5`, com `no-explicit-any`, `no-unsafe-assignment/member-access/call/return`, `complexity:10`, `max-depth:3`, `eqeqeq`, `prefer-const`, `no-console` — toolchain instalada **fora do repositório** (no diretório de trabalho da sessão). Resultado: **1 achado**, `@typescript-eslint/consistent-type-definitions` em `src/config/env.ts:13` (preferir `interface` a `type`) — opinião do preset *stylistic*, deliberadamente recusada (ver DECISÃO-005). **Zero** achados de tipagem, complexidade, profundidade ou uso indevido de hooks. A configuração efêmera foi removida do repositório (confirmado por `find`); nada foi adicionado ao `package.json`.
- [~] Run tests to ensure all implemented code is covered and all tests are passing successfully.
  - **Fora do escopo por decisão explícita da task** ("Out: ... Sem Jest (TASK-FRONTEND-013)") e das restrições do slice (proibido adicionar Jest). Compensado por verificação executável no navegador com medição real — ver seção *Testing and Validation*.
- [x] Run complexity check in MCP, if available, to ensure the generated code does not exceed the project's complexity standards.
  - `complexity-check(path=services/frontend/src, cyclomaticThreshold=10)`: **nenhuma função acima do limiar**. A resposta do MCP nesse caso é um registro degenerado (`filePath: ""`, `functionName: "Não identificado"`, valores 0) — **confirmei que é o formato de "nada encontrado"** rodando a mesma chamada contra `services/backend/src`, que produz resposta idêntica e cujo resultado já era conhecido como limpo na TASK-BACKEND-001.
  - Confirmado de forma independente pela regra `complexity: ['error', 10]` e `max-depth: ['error', 3]` do ESLint efêmero: **0 violações**.
- [!] Run SonarQube analysis using the Makuco MCP tools, if applicable, to ensure that the generated code meets the project's quality standards and does not introduce new issues.
  - **NÃO EXECUTADO — servidor inacessível em 2026-08-22.** `get-sonar-issues` retornou `ETIMEDOUT`; `curl http://sonar.anymarket.vpc:9000/api/system/status` retornou `status=000` após 15s. **Nenhuma métrica é declarada aqui, porque nenhuma foi medida.**
  - Diagnóstico preciso (para não repetir o erro de atribuição da TASK-BACKEND-001, que primeiro culpou a VPN): a VPN **está ativa** (`tun0` UP), o nome **resolve** (`10.119.10.55`, via `/etc/hosts`) e **existe rota** para ele (`ip route get 10.119.10.55` → `via 10.200.52.1 dev tun0`). O que falha é o **handshake TCP na porta 9000** (`/dev/tcp/sonar.anymarket.vpc/9000` expira em 8s). Ou seja: não é DNS, não é ausência de VPN e não é o bug do `--network host` do MCP documentado na TASK-BACKEND-001 — é o **servidor ou um filtro no caminho** não aceitando conexão neste momento. Mesmo estado observado nas TASK-BACKEND-004..007.
  - **Compensação executada**: rodei localmente o `eslint-plugin-sonarjs@4.2.0`, que é a implementação das regras JS/TS usadas pelo próprio analisador do SonarQube. Perfil `recommended` = **279 regras**, aplicado a `src/**/*.{ts,tsx}` (com informação de tipos, via `tsconfig.json`) e aos configs da raiz (`tailwind.config.js`, `postcss.config.js`, `vite.config.ts`, sem tipos). Resultado: **0 achados, exit 0**. Config efêmera removida.
  - **Limite honesto dessa compensação**: o `eslint-plugin-sonarjs` cobre as regras de código, mas **não** entrega quality gate, cobertura, densidade de duplicação, security hotspots nem a análise do perfil `Web` sobre o `index.html`. Esses quatro permanecem **não medidos**.
  - **Pendência registrada (não corrigida por estar fora da tabela *Files*)**: o `sonar-project.properties` da raiz declara hoje `sonar.sources=services/backend/src` e `sonar.tests=services/backend/tests` **apenas** — deliberadamente, porque `services/frontend/` estava vazio e o scanner **aborta** com caminho inexistente (o próprio arquivo documenta isso). **Depois desta task o diretório existe.** Para o frontend entrar na análise será preciso acrescentar `services/frontend/src` a `sonar.sources`. Dois detalhes que a TASK-FRONTEND-013 precisa observar ao fazer isso: (a) `sonar.javascript.lcov.reportPaths` **já** cita `services/frontend/coverage/lcov.info`, então essa metade está pronta; (b) `services/frontend/src` **não** cobre o `index.html`, que vive na raiz do serviço — se o perfil `Web` tiver de analisá-lo, o caminho declarado precisa ser `services/frontend` com exclusões, não `services/frontend/src`.

## Code Quality

- [x] Code follows the project's existing patterns and best practices.
  - Aderência verificada contra `.makuco/codebase/conventions.md`, `stack.md` e o backend já commitado: arquivos em kebab-case (`env.ts`, `index.css`), componente React em PascalCase (`App.tsx`), alias `~/` nos imports internos (`~/App`, `~/styles/index.css`), pasta `config/` com o mesmo papel que no backend (ponto único de leitura de ambiente), **comentários em PT-BR sem acento** e **texto visível ao usuário com acento** (`"Design system carregado. Este cartão é um placeholder de verificação dos tokens."`), `<html lang="pt-BR">`.
  - Versões conforme `stack.md`: React 18.3.1, Vite 5.4.21, Tailwind 3.4.19, TypeScript 5.9.3.
  - `engines: {node: ">=20 <21"}` replicado do backend, coerente com o `.nvmrc` da raiz.
  - Nenhum comentário acentuado (`grep -P` por vogais acentuadas em linhas de comentário: 0 ocorrências). Nenhuma ocorrência da palavra `todo` isolada em comentário — o falso positivo `typescript:S1135` que já custou cinco correções neste projeto foi evitado por construção (`grep -riwE "todo"`: 0 ocorrências).
- [x] Code is free of linting and compiler errors.
  - `tsc --noEmit` = 0 erros; ESLint efêmero = 0 erros de correção (1 preferência estilística recusada, justificada); `eslint-plugin-sonarjs` (279 regras) = 0 achados.
- [x] Code is readable and maintainable, with clear naming conventions and structure.
  - 11 arquivos, 315 linhas no total. O maior é `tailwind.config.js` com 73 linhas — e é declarativo, não lógica. Os quatro arquivos de `src/` têm 29, 30, 22 e 31 linhas.
  - Responsabilidade única por arquivo: `env.ts` isola o ambiente, `main.tsx` monta, `App.tsx` é o placeholder de verificação, `index.css` são as camadas base, `tailwind.config.js` são os tokens.
  - Cada decisão não óbvia está comentada **no ponto de uso**: o alias em dois lugares (`vite.config.ts`), o motivo do `strictPort`, o proxy como mesma origem, o `animation: none` em vez de duração reduzida, o motivo arquitetural do ponto único de `import.meta.env`, o `both` do fill mode, e o mapeamento token → variável CSS de origem linha a linha no `tailwind.config.js`.
- [!] Zero new issues introduced in SonarQube analysis (if applicable).
  - **Não verificável**: SonarQube inacessível (ver *Quality Tools*). Não declaro contagem de issues que não medi.
  - Melhor evidência disponível: 0 achados nas 279 regras do `eslint-plugin-sonarjs` sobre `src/` e os configs.
  - **Previsão registrada, não medida**: quando o frontend entrar no escopo do scanner, o `<link>` do Google Fonts no `index.html` provavelmente disparará `Web:S5725` ("recurso externo sem `integrity`"), a mesma regra já ignorada nos templates de e-mail. Não é defeito: SRI em folha de estilo do Google Fonts é contraindicado, porque o CSS servido varia por user-agent e o hash quebraria. Decisão deixada para quem alterar o `sonar-project.properties`.
- [x] No code duplication introduced (DRY principle).
  - Nenhum bloco repetido entre arquivos (`sort | uniq -d` sobre as linhas não vazias e não comentadas de `src/`: apenas `/**` e `}`).
  - Os valores visuais existem em **um** lugar (`tailwind.config.js`) e são consumidos por classe utilitária — é justamente o oposto da duplicação que ocorreria copiando o CSS do mockup. Nenhum literal de cor, raio ou sombra aparece em `App.tsx`, `main.tsx` ou `index.css`.
- [x] No GOD classes, methods or files introduced.
  - Nenhum arquivo acima de 73 linhas; nenhuma função acima de 20 linhas; complexidade ciclomática máxima abaixo de 10 (medida). `App.tsx` tem um único componente sem estado e sem ramificação.
- [~] Code is properly tested, with all tests passing and at least 80% of coverage.
  - Testes automatizados são a TASK-FRONTEND-013 por decisão da task (Jest proibido neste slice). **Cobertura automatizada atual: 0%** — declarado explicitamente, não presumido. Todo comportamento entregue foi verificado no navegador com medição real (ver *Testing and Validation*).
  - O código foi escrito para ser testável na 013: `env.ts` é o único ponto de `import.meta.env` (sintaxe inválida sob a transformação CommonJS do Jest), portanto é o único módulo que precisará de `moduleNameMapper`.

## Security Check

- [!] No new vulnerabilities introduced in SonarQube analysis.
  - **Não verificável** (SonarQube inacessível). Substituto executado: `npm audit`.
  - `npm audit` em 137 pacotes: **4 vulnerabilidades (3 moderate, 1 high)**. Nenhuma delas é código deste slice — todas vêm das **majors prescritas pelo contrato da task e pelo `stack.md`**:
    - `vite@5.4.21` (**high**: `server.fs.deny` bypass em caminhos alternativos do Windows; **moderate**: path traversal no tratamento de `.map` de deps otimizadas, `launch-editor` NTLMv2 em UNC no Windows) e sua dependência `esbuild<=0.24.2` (**moderate**: qualquer site pode requisitar o dev server e ler a resposta).
    - `react-router@6.30.6` via `react-router-dom@^6` (**moderate**: open redirect por barra invertida em `<Link>`/`useNavigate`; injeção de construtor em `deserializeErrors()` na hidratação SSR).
  - **Nenhuma tem correção dentro das majors permitidas**: `npm audit fix --force` propõe `vite@8.2.2` e `react-router-dom@7.18.2`, ambos *breaking* e ambos violando `stack.md` (Vite 5.x) e a tabela de dependências da task (`react-router-dom@^6`). `5.4.21` **já é a última** da linha 5.x. **Não corrigido de propósito**, e registrado como risco.
  - Atenuação factual: as três de `vite`/`esbuild` afetam o **dev server**, que só escuta em `localhost` (sem `--host`), e duas são específicas de Windows; esta máquina é Linux. As duas de `react-router` só se tornam alcançáveis quando houver rotas — e **não há nenhuma neste slice** (`react-router-dom` está instalado por exigência do contrato, porém não é importado por nenhum módulo; a TASK-FRONTEND-011 é quem o consome, e deve reavaliar a advisory de open redirect antes de usar `<Link>` com destino vindo de parâmetro).
- [x] All inputs are validated at system boundaries to prevent injection attacks and ensure data integrity.
  - Este slice tem **uma** fronteira de entrada: `import.meta.env.VITE_API_BASE_URL`, lida exclusivamente em `src/config/env.ts`, com default `/api` via `??` e tipo declarado `string | undefined` (não há valor implícito). O objeto resultante é `Object.freeze`, confirmado no bundle de produção (`Object.freeze({apiBaseUrl:"/api"})`).
  - Não há entrada de usuário: sem formulário, sem parâmetro de rota, sem leitura de `location`, sem `localStorage`, sem chamada de API. Validação de payload de autenticação pertence às TASK-FRONTEND-009/010/012.
- [!] No security hotspots introduced in SonarQube analysis.
  - **Não verificável** (SonarQube inacessível). Revisão manual e automatizada consistente com ausência de hotspot: sem `eval`, sem `new Function`, sem `dangerouslySetInnerHTML`, sem `innerHTML`, sem `document.write`, sem `postMessage`, sem `target="_blank"`, sem criptografia própria, sem `http://` em conteúdo (as duas URLs externas são `https://fonts.googleapis.com` e `https://fonts.gstatic.com`). As 279 regras do `sonarjs`, que incluem a família de segurança de JS/TS, não acusaram nada.
  - Observação deliberada: o `<script>` gerador de pegadas do `reference.html` **não** foi trazido (ele usa `innerHTML` com template string e `Math.random`). O mockup entrou só como tabela de valores.
- [x] Code does not contain any known security anti-patterns (e.g., hardcoded secrets, unsafe deserialization, etc.).
  - Nenhum segredo no código: não há `.env` no frontend, e o `.gitignore` do serviço cobre `.env` desde já, junto com `node_modules/`, `dist/` e `coverage/`.
  - Nenhum endereço de API embutido em código de aplicação: o único alvo absoluto (`http://localhost:3333`) está em `vite.config.ts`, que é configuração de **desenvolvimento** e não vai para o bundle. Em produção o destino vem de `VITE_API_BASE_URL`.
  - Nenhuma desserialização: não há `JSON.parse` em nenhum arquivo do slice.
- [x] Code follows secure coding practices as defined by the project and industry standards.
  - O proxy `/api` do Vite existe justamente por uma razão de segurança de cookie: mantém a chamada como **mesma origem** em desenvolvimento, e assim o cookie de refresh que a TASK-BACKEND-005 emite chega como *first-party*, **sem** exigir `SameSite=None` (que o desligaria da proteção contra CSRF por navegação cross-site). Comportamento **comprovado**, não assumido — ver AC 2 em *Testing and Validation*.
  - `server.port: 5173` + `strictPort: true` garante que a origem do dev server seja exatamente a declarada em `CORS_ALLOWED_ORIGINS` e `APP_WEB_URL` do backend (ambas `http://localhost:5173`, conferidas no `.env` real). Sem `strictPort`, o Vite subiria na 5174 e o backend passaria a **rejeitar** a origem — falha silenciosa; ver DECISÃO-001.
  - `import.meta.env` não é espalhado, o que impede que uma variável de ambiente vaze para um módulo qualquer sem revisão.
- [x] No security vulnerabilities introduced (e.g., injection, XSS, SSRF, etc.)
  - XSS: todo texto renderizado é literal em JSX (escapado pelo React); nenhuma API de HTML cru foi usada.
  - SSRF: não há requisição de saída no código de aplicação. O proxy do Vite tem destino **fixo** (`http://localhost:3333`), sem nenhuma parte derivada de entrada.
  - Injeção: sem SQL, sem shell, sem construção dinâmica de código.
  - As 4 vulnerabilidades do `npm audit` são de dependência e estão detalhadas no primeiro item desta seção, com o motivo de não serem corrigíveis dentro do contrato.

## Implementation Completeness

- [x] All steps in the execution plan have been implemented as specified.
  - Os **11 arquivos** da tabela *Files* criados, nenhum a mais, nenhum a menos: `package.json`, `vite.config.ts`, `tsconfig.json`, `tailwind.config.js`, `postcss.config.js`, `.gitignore`, `index.html`, `src/styles/index.css`, `src/config/env.ts`, `src/main.tsx`, `src/App.tsx`.
  - Contrato de dependências respeitado ao pé da letra. Presentes: `react@18.3.1`, `react-dom@18.3.1`, `react-router-dom@6.30.6`, `vite@5.4.21`, `@vitejs/plugin-react@4.7.0`, `typescript@5.9.3`, `tailwindcss@3.4.19`, `postcss@8.5.26`, `autoprefixer@10.5.4`, `@types/react@18.3.31`, `@types/react-dom@18.3.7`. **Ausentes conforme a proibição**: `axios`, `jest`, `ts-jest`, `@types/jest`, qualquer biblioteca de UI. Nada além da lista foi instalado.
  - Scripts conforme o plano: `dev`, `build` (`tsc --noEmit && vite build`), `preview`, `typecheck`.
- [x] All necessary files have been created and properly structured.
  - `services/backend/` **não foi alterado**: `git status` mostra apenas `services/frontend/` e este checklist como não rastreados; nenhum arquivo do backend modificado. O backend só foi **construído e executado** (`npm run build`, `node dist/index.js`) para provar o proxy, e `dist/` é gitignorado.
  - Nenhum arquivo fora de escopo: sem componente de UI, sem rota, sem guard, sem layout, sem página, sem contexto de autenticação, sem cliente HTTP, sem teste. Sem `BrowserRouter` em `main.tsx` (verificado) — ele pertence à TASK-FRONTEND-011.
  - **Nenhum CSS do `reference.html` foi copiado.** `grep` por `var(--`, `position: absolute/fixed`, `display: flex` como declaração, `box-shadow:`, `@keyframes` e pelos seletores do mockup (`.paw`, `.btn-submit`, `.eye-btn`, `.logo-name`) nos `.css`/`.ts`/`.tsx`/`.html` do serviço: **0 ocorrências**. As 21 variáveis CSS do mockup viraram tokens nomeados no `tailwind.config.js`; o `<script>` das pegadas não foi trazido.
- [x] All referenced code patterns and best practices have been followed.
  - Rastreabilidade token → mockup conferida **programaticamente**, valor por valor: as **13 cores** (`#7c3aed`, `#6d28d9`, `#ede9fe`, `#e05a1e`, `#c44a10`, `#1e1b2e`, `#4b4869`, `#9896b0`, `#ffffff`, `#f8f7fc`, `#dde0ea`, `#e4e2f0`, `#b0aec8`) e os **11 valores** não cromáticos (`22px`, `14px`, `420px`, `44px`, `0.55s`, `cubic-bezier(.22,1,.36,1)`, `translateY(28px)`, `0 8px 40px`, `0 4px 16px`, `0 6px 20px`, `0 0 0 3px`) estão **presentes no `reference.html` E no `tailwind.config.js`**, sem exceção. Os pesos `wght@400;600;700;800` batem entre `reference.html` e `index.html`. **Nenhuma divergência entre a task e a referência normativa** — nada precisou ser resolvido em favor da referência.
  - Namespaces próprios (`brand`, `ink`, `surface`, `hairline`, `paw`) conforme prescrito: nenhuma escala padrão do Tailwind foi sobrescrita (`purple` e `gray` do Tailwind seguem intactas).
  - Alias `~/` declarado nos **dois** lugares exigidos e provado nos dois (ver AC 7).
- [x] All validation rules have been implemented and passed successfully.
  - A única regra de validação do slice é o fallback do ambiente. Comprovada nos dois sentidos: sem `.env`, `env.apiBaseUrl` resolve para `/api` (valor congelado observado no bundle de produção); e o tipo é `string | undefined` na leitura, não um tipo frouxo — comprovado por **teste de contraste** (ver DECISÃO-003).

## Testing and Validation

- [~] All implemented code is covered by tests, including edge cases.
  - Sem testes automatizados por decisão da task (TASK-FRONTEND-013). **Verificação executada no navegador, com medição real e reproduzível.** Os 10 critérios de aceite, um a um:

  1. **AC 1 — `npm run dev` sobe na 5173 e renderiza sem erro no console.** `[x]` Dev server em `http://localhost:5173/` (`VITE v5.4.21 ready in 414 ms`, socket confirmado por `ss -ltnp`). Carga limpa do placeholder: **0 erros, 0 avisos** de console (`page.on('console')` + `page.on('pageerror')` em página nova). Achado durante a verificação: a primeira execução tinha **1 erro** — `GET /favicon.ico 404`, pedido espontâneo do navegador. Corrigido no `index.html` (DECISÃO-002) e reverificado.
  2. **AC 2 — `/api/health` proxeado e tratado pelo navegador como MESMA ORIGEM (sem preflight).** `[x]` **Provado com o backend real na 3333** e com controle experimental, via CDP (`Network.enable` + `Network.requestWillBeSent`), porque o `page.on('request')` do Playwright **não** expõe preflight — a primeira tentativa deu falso "0 OPTIONS" nos dois lados e teria sido uma conclusão errada.
     - `GET /api/health` pelo dev server → `200 {"status":"ok","uptime":...}`, `new URL(r.url).origin === location.origin` → **true**, e `r.headers.get('access-control-allow-origin')` → **null**. Em resposta de mesma origem **todos** os headers são legíveis por JS; a ausência do header prova que o backend não recebeu `Origin`, isto é, o navegador não tratou a chamada como CORS.
     - Controle decisivo: **a mesma** requisição não simples (`POST` + `Content-Type: application/json` + `credentials: 'include'`) disparada duas vezes, mudando só a URL. Pelo proxy (`localhost:5173/api/health`): **0 preflight**, nenhum `Origin`. Direto na origem cruzada (`localhost:3333/api/health`): **1 `OPTIONS`** com `Origin: http://localhost:5173` e `Sec-Fetch-Mode: cors`. Mesmo navegador, mesma sessão, mesmo verbo, mesmo header — só a URL diferente. É o proxy que elimina o CORS.
     - `PORT=3333` e `CORS_ALLOWED_ORIGINS=http://localhost:5173` conferidos no `.env` real do backend; `APP_WEB_URL` tem default `http://localhost:5173` no schema Zod de `services/backend/src/config/env.ts`. As três origens batem com `server.port`.
     - O processo do backend subido para esta prova **foi encerrado** ao final (ver *Notes*).
  3. **AC 3 — fundo `#dde0ea`, card branco raio 22px e sombra `0 8px 40px rgba(100,80,180,0.13)`, família Nunito.** `[x]` Estilo **computado** medido no navegador: `body.backgroundColor = rgb(221, 224, 234)` (= `#dde0ea`), `body.color = rgb(30, 27, 46)` (= `#1e1b2e`), `card.backgroundColor = rgb(255, 255, 255)`, `card.borderRadius = 22px`, `card.boxShadow` contém `rgba(100, 80, 180, 0.13) 0px 8px 40px 0px`, `card.padding = 44px`, `card.maxWidth = 420px`, `fontFamily = Nunito, system-ui, -apple-system, "Segoe UI", Arial, sans-serif`, `-webkit-font-smoothing = antialiased`, `<html lang="pt-BR">`, `document.title = "CatDog"`. Confirmado também no **build de produção** via `vite preview` (`localhost:4173`), com resultados idênticos e 0 erro de console. Screenshot de registro conferida visualmente: cartão branco centralizado sobre o cinza-azulado, tipografia Nunito, acentuação PT-BR correta.
     - Fonte: `document.fonts.load('700 16px Nunito')` → 1 face carregada; `document.fonts.check` verdadeiro para 600, 700 e 800. O peso 400 aparece como `unloaded` **porque nenhum elemento do placeholder o usa** — é o carregamento tardio do navegador, não declaração faltando: as faces dos quatro pesos estão presentes em `document.fonts`.
  4. **AC 4 — o card executa `fadeUp` (opacidade 0→1, deslocamento de 28px) uma única vez.** `[x]` Contagem de eventos instalada **antes de qualquer script da página** (`addInitScript`), em página nova sem instrumentação acumulada: exatamente **1 `animationstart` e 1 `animationend`** de `fadeUp`, **0 `animationiteration`**. Computado: `animation-name: fadeUp`, `animation-duration: 0.55s`, `animation-timing-function: cubic-bezier(0.22, 1, 0.36, 1)`, `animation-iteration-count: 1`, `animation-fill-mode: both`. Keyframes no CSS gerado: `@keyframes fadeUp{0%{opacity:0;transform:translateY(28px)}to{opacity:1;transform:translateY(0)}}`. Isto também responde a uma dúvida legítima do `StrictMode`: o duplo render de desenvolvimento **não** reexecuta a animação, porque o React reaproveita o nó do DOM.
     - Nota metodológica: uma medição intermediária registrou "2 starts". Era **artefato do instrumento** — dois `addInitScript` acumulados na mesma página, portanto dois listeners contando o mesmo evento. Refeita em página limpa, deu 1. Registro o erro em vez de omiti-lo.
  5. **AC 5 — com "reduzir movimento" ativo, nenhuma animação é executada.** `[x]` Com `emulateMedia({ reducedMotion: 'reduce' })`: `matchMedia('(prefers-reduced-motion: reduce)').matches = true`, `animation-name: none`, `animation-duration: 0s`, `transition-property: none`, `document.getAnimations().length = 0` e **lista de eventos de animação vazia**. Com `no-preference`, a mesma página volta a `animation-name: fadeUp` / `0.55s`. O bloco `@media` usa `animation: none !important` — **não** duração reduzida, que ainda dispararia os eventos e ainda contaria como animação executada. O CSS gerado confirma: `@media (prefers-reduced-motion: reduce){*,*:before,*:after{animation:none!important;transition:none!important;scroll-behavior:auto!important}}`.
  6. **AC 6 — as 7 classes resolvem para os valores do `reference.html`.** `[x]` Verificado no **CSS gerado** (`dist/assets/index-*.css`), não só na config, e reconfirmado por estilo computado no navegador: `.bg-brand-purple` → `rgb(124 58 237)` = `#7c3aed`; `.text-ink-muted` → `rgb(152 150 176)` = `#9896b0` (computado no `<p>`: `rgb(152, 150, 176)`); `.rounded-field` → `border-radius:14px`; `.shadow-button` → `0 4px 16px rgba(124, 58, 237, .3)` (computado: `rgba(124, 58, 237, 0.3) 0px 4px 16px`); `.max-w-card` → `max-width:420px`; `.p-card` → `padding:44px`; `.animate-fadeUp` → `animation:fadeUp .55s cubic-bezier(.22,1,.36,1) both`. As sete aparecem no CSS **porque o placeholder as usa de fato** — o Tailwind só emite a regra de classe presente no `content`, então uma config correta com placeholder que não as usasse produziria CSS sem nada a inspecionar.
  7. **AC 7 — `import { env } from '~/config/env'` resolve no `tsc --noEmit` e no build do Vite.** `[x]` Provado com o **import literal do AC**, por sonda efêmera (necessária porque nenhum módulo do slice importa `env` ainda — quem consome é a TASK-FRONTEND-010): arquivo temporário com `import { env } from '~/config/env'` → `tsc --noEmit` **0 erros** e `vite build` **33 módulos** (contra 31 sem a sonda, isto é, `env.ts` entrou no grafo). O bundle emitido contém `Object.freeze({apiBaseUrl:"/api"})`, ou seja, o módulo foi resolvido, compilado e o `import.meta.env` substituído estaticamente. Sonda **removida** e projeto reconstruído (`grep` por vestígio em `src/` e `dist/`: 0 ocorrências).
     - O alias também é exercitado permanentemente por `main.tsx` (`~/App`, `~/styles/index.css`), que passa nos dois caminhos no build normal.
  8. **AC 8 — `import.meta.env` só em `src/config/env.ts`.** `[x]` `grep -rn "import\.meta" services/frontend/src/`: **5 ocorrências, todas no mesmo arquivo** — 4 em comentário explicando a restrição e **1 de código** (`src/config/env.ts:28`). Busca fora de `src/` nos `.ts`/`.tsx`/`.js`/`.html` do serviço: **0 ocorrências**.
  9. **AC 9 — `npm run build` com 0 erros de tipo e `dist/` gerado.** `[x]` `tsc --noEmit && vite build` → 0 erros, `✓ 31 modules transformed`, `dist/index.html` (1.20 kB), `dist/assets/index-D5AHlQ1L.css` (6.99 kB), `dist/assets/index-Dmzl6TfM.js` (143.18 kB). O `dist/` foi validado além da existência: servido por `vite preview` e medido no navegador com resultado idêntico ao dev e **0 erro de console**.
  10. **AC 10 — nenhum `any`; nenhum CSS do `reference.html` copiado literalmente.** `[x]` `grep -rnw "any"` em `services/frontend/src/`: **0 ocorrências** (a única menção existente, em comentário, foi reescrita justamente para não haver ambiguidade). `grep` por `as any`, `<any>`, `: any`, `any[]`, `Array<any>`, `@ts-ignore` e `@ts-expect-error` no serviço: **0**. Confirmado por ferramenta, não só por busca: `@typescript-eslint/no-explicit-any` **e** as quatro regras `no-unsafe-*` do preset *typeChecked* passaram limpas — o que só é possível porque a augmentação de `ImportMetaEnv` existe (DECISÃO-003). CSS do mockup: 0 ocorrências (detalhado em *Implementation Completeness*).

- [~] All tests are passing successfully.
  - Não há suíte de testes neste slice (fora de escopo por decisão da task). A separação que a viabiliza já está feita: `env.ts` isolado como único ponto de `import.meta.env`.
- [!] SonarQube analysis shows no new issues introduced by the generated code (if applicable).
  - **Não verificável**: servidor inacessível (`ETIMEDOUT`). Nenhum número inventado. Compensação parcial e seus limites descritos em *Quality Tools*.
- [~] Tests cover expected behavior and edge cases, ensuring the implementation is robust and reliable, covering validation rules defined in the prompt plan.
  - As regras foram exercitadas **manualmente com medição instrumentada** (acima), não por testes automatizados. Converter isso em suíte é a TASK-FRONTEND-013. Dois dos casos de borda verificados aqui merecem virar teste lá: o fallback `/api` do `env` (com e sem `VITE_API_BASE_URL`) e o comportamento sob `prefers-reduced-motion`.

## Notes

- **DECISÃO-001 — `server.strictPort: true` acrescentado ao `vite.config.ts`.** Não consta no plano, que prescreve apenas `server.port: 5173`. Sem ele, uma 5173 ocupada faz o Vite subir na **5174** e o backend passa a **rejeitar** a origem, porque `CORS_ALLOWED_ORIGINS` é exatamente `http://localhost:5173` — falha silenciosa, que aparece como "login não funciona" e não como "porta errada". Com `strictPort`, o dev server falha no boot dizendo o motivo. **Comprovado por acidente durante a verificação**: uma tentativa de reiniciar o dev server com a porta ainda ocupada produziu `Error: Port 5173 is already in use` em vez de migrar de porta. Custo zero, e protege o contrato de origem que a AC 2 depende.
- **DECISÃO-002 — `<link rel="icon" href="data:," />` no `index.html`.** A AC 1 exige carga **sem erro no console** e a primeira execução tinha exatamente um: `GET /favicon.ico 404`, pedido que o navegador faz por conta própria quando a página não declara ícone. As alternativas eram criar `public/favicon.svg` (arquivo **fora da tabela *Files***, e o ícone da marca pertence aos assets de identidade visual da TASK-FRONTEND-009) ou deixar o erro (violaria a AC). O `data:,` é declaração vazia dentro de um arquivo que **já** está na tabela, não adiciona arquivo nenhum e é substituível por uma linha quando o ícone real existir. Comentado no ponto de uso.
- **DECISÃO-003 — augmentação de `ImportMetaEnv` dentro de `src/config/env.ts`.** A `ImportMetaEnv` de `vite/client` tem assinatura de índice frouxa, então `import.meta.env.VITE_API_BASE_URL` chega como tipo não definido e **violaria a AC 10 na prática**, mesmo sem a palavra `any` aparecer no código. Declarei a chave via `declare global { interface ImportMetaEnv { readonly VITE_API_BASE_URL?: string } }` **no próprio `env.ts`**, e não em um `src/vite-env.d.ts` — que é o padrão do scaffold do Vite, mas seria um **12º arquivo fora da tabela *Files***. **Comprovei que a augmentação é carregadora de peso, não decoração**: removendo apenas esse bloco, o ESLint efêmero passa a acusar `@typescript-eslint/no-unsafe-assignment` — "Unsafe assignment of an `any` value" — na atribuição de `apiBaseUrl`; recolocando, o achado desaparece. Teste de contraste executado nos dois sentidos.
- **DECISÃO-004 — `tsconfig.json` com `include: ["src"]`, deixando `vite.config.ts` fora do `tsc --noEmit`.** O plano prevê **um** `tsconfig.json`, com `types: ["vite/client"]`. Incluir `vite.config.ts` exigiria `@types/node` (por causa de `node:path` e `__dirname`), e `@types/node` **não está na lista de dependências permitidas**. O scaffold oficial do Vite resolve isso com um segundo arquivo (`tsconfig.node.json`), que também está fora da tabela *Files*. Consequência aceita e registrada: `vite.config.ts` é transpilado pelo esbuild sem checagem de tipos. Mitigação executada: `vite.config.ts` foi verificado pelas 279 regras do `eslint-plugin-sonarjs` (0 achados) e, o que importa mais, o `__dirname` do alias foi **provado em execução** — se ele fosse `undefined` sob o carregamento ESM do config (o `package.json` tem `"type": "module"`), `path.resolve` lançaria `TypeError` e o build falharia; o build passa e o alias resolve. Recomendação para a TASK-FRONTEND-013: ao trazer o Jest, avaliar um `tsconfig.node.json` para cobrir os arquivos de configuração.
- **DECISÃO-005 — `export type Env` mantido, contrariando `@typescript-eslint/consistent-type-definitions`.** Único achado do ESLint efêmero. Duas razões: (a) coerência com o backend, cujo `src/config/env.ts` também exporta `export type Env`; (b) e a razão de fundo — `interface` é **aberta a declaration merging** e `type` não. No mesmo arquivo eu uso `interface` de propósito para `ImportMetaEnv`, exatamente porque **quero** que ela seja mesclada com a de `vite/client`. `Env` é o oposto: contrato fechado, que ninguém deve estender de fora. A regra vem do preset *stylistic* de um linter que **não é política deste projeto** (não há ESLint no repositório), então o achado é opinião de preset, não defeito.
- **DESVIO-006 — `services/frontend/package-lock.json` foi versionado.** Não consta na tabela *Files*, mas é o produto natural do `npm install` exigido pela task e o backend versiona o seu (`services/backend/package-lock.json` está no repositório). Manter é o que torna a instalação reprodutível: sem o lock, `npm ci` não funciona e cada dev — e o CI — resolveria uma árvore diferente dentro dos mesmos intervalos `^`, o que é especialmente relevante aqui, dado o RISCO-D (as versões exatas de `vite` e `react-router` importam para a leitura do `npm audit`). **DECIDIDO em 2026-08-22: manter.** `node_modules/` e `dist/` seguem ignorados, confirmado por `git check-ignore`.
- **OBSERVAÇÃO-A — `spacing.card` é `44px`, mas o `.card` do mockup tem `padding: 44px 44px 36px`.** O plano prescreve um único token de `44px` e a AC 6 cobra `p-card`; ambos cumpridos. O registro existe porque a **base do cartão no mockup é 36px, não 44px** — quem montar o cartão real na TASK-FRONTEND-009 deve usar `p-card` com a base sobrescrita (`pb-9` = 36px) para reproduzir a referência com fidelidade. O comentário no `tailwind.config.js` já anota isso na linha do token.
- **OBSERVAÇÃO-B — `src/config/env.ts` e `react-router-dom` ainda não são importados por nenhum módulo.** São infraestrutura **exigida pelo contrato** desta task e consumidas a partir das TASK-FRONTEND-010 e 011. Não é código morto acidental — é a mesma situação do `src/infra/prisma/prisma-client.ts` na TASK-BACKEND-001. Consequência prática: `env.ts` não entra no bundle de produção hoje (o Vite faz tree-shaking do que ninguém importa), o que foi confirmado ao contar os módulos com e sem a sonda da AC 7 (33 contra 31).
- **OBSERVAÇÃO-C — bug no MCP `quality-check`, a reportar ao time do Makuco.** O comando montado foi `npx eslint eslint --fix services/frontend/src --cache`: (a) o nome do binário aparece **duplicado** como primeiro argumento posicional; (b) `--fix` é passado **incondicionalmente**, mesmo sem o parâmetro `fix` — ou seja, uma chamada de *verificação* reescreveria arquivos-fonte sem que ninguém tivesse pedido; (c) o `tsc` é invocado a partir da raiz do repositório, onde não há TypeScript instalado (é por serviço), resultando em "This is not the tsc command you are looking for". Neste projeto o item (b) é inócuo porque não há config de ESLint, mas em repositório com ESLint configurado seria mutação silenciosa. Complementa o bug de `--network host` do `sonar-run` já registrado na TASK-BACKEND-001.
- **RISCO-D — `npm audit`: 4 vulnerabilidades sem correção dentro do contrato.** 3 moderate + 1 high, todas em `vite@5`/`esbuild` e `react-router@6`, detalhadas em *Security Check*. `5.4.21` já é a última da linha 5.x; corrigir exige `vite@8` e `react-router-dom@7`, ambos *breaking* e ambos contra o `stack.md` (Vite 5.x) e a tabela de dependências da task. **Decisão: não corrigir neste slice** — a mudança de major é decisão de arquitetura, não de scaffolding. Duas consequências práticas: o dev server só deve ser exposto com `--host` em rede confiável, e a TASK-FRONTEND-011 deve tratar a advisory de open redirect do `react-router` antes de navegar para destino vindo de parâmetro de URL.
- **PENDÊNCIA-E — `sonar-project.properties` precisará incluir `services/frontend/src`.** **Não alterado**, por não constar na tabela *Files*. Detalhamento completo (incluindo a armadilha do `index.html` ficar fora de `src/`) no item de SonarQube em *Quality Tools*.
- **Estado do ambiente ao encerrar.** Os processos subidos nesta sessão foram todos encerrados: backend (`node dist/index.js`, 3333), `vite dev` (5173) e `vite preview` (4173) — cada PID identificado por `ps` e confirmado como iniciado por esta sessão antes do `kill`; nenhum processo do desenvolvedor foi tocado. Portas 3333, 5173 e 4173 verificadas livres depois. Nenhum arquivo temporário no repositório: sonda de alias, dois configs efêmeros de ESLint, o screenshot (que o Playwright gravou na raiz do repositório, **fora** do `.gitignore`) e os logs de `.playwright-mcp/` foram removidos ou movidos para fora — `find` de conferência por `*.bak`, `*efemero*`, `*sonda*`, `.eslintcache`, `*.png` e `nohup.out`: **nenhum resíduo**. `git status` final lista exatamente os 11 arquivos da tabela *Files* + `package-lock.json` (DESVIO-006) + este checklist. A aba do navegador aberta por mim foi fechada; a aba do desenvolvedor (Supabase) permaneceu intacta, e o viewport nunca foi alterado. `services/backend/` sem modificação de fonte.
- **Sem commit e sem push**, por instrução explícita. Nenhuma branch criada — trabalho deixado no *working tree* de `master`, coerente com a TASK-BACKEND-001, que também dispensou a branch prevista no cabeçalho da task.
